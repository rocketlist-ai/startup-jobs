# Security

Do not open a public issue for a credential exposure, personal-data leak, or vulnerability. Report it privately through GitHub's security-advisory form for this repository.

The public dataset is produced from an explicit field allowlist. It intentionally excludes job descriptions, raw ATS payloads, embeddings, user records, matching data, prompts, and internal crawler metadata.

